# D2I Cloud 现状盘点与迭代方案(2026-07-26)

> 本文由 2026-07-26 晚的一次全仓体检产出(9 路并行审计:git 状态 / cloud 子系统 / 爬虫核心 / 契约一致性 / Vision 专项 / 安全面 / 仓库文档 / 知识库文档 / 部署工艺,全程只读、未连 NAS、未读凭据)。
> 定位:**当前唯一的"下一步怎么迭代"总纲**。问题均附代码位置,修一项勾一项。

---

## 0. 一句话现状

**产品主链路(模板 → Cloud 建队 → safe 抓取 → 姓名净化 → 元数据 → 多级落盘 → 手动 finalize → 角色肖像 + people 回写)已经真实跑通**(沧州 T0:6 人,6/6 姓名、6/6 图、6/6 元数据,promoted 成功);剩下的是**让闭环全自动、把单点隐患消掉、把模板库和文档口径拉齐**。

---

## 1. 已跑通 / 已确认没问题的

| 项 | 结论 | 依据 |
| --- | --- | --- |
| T0 五个核心修复已固化 | `run_public_scraper.py`/`public_profile_spider.py`/`task_service.py`/`public_scraper_config_service.py`/沧州模板 全部已提交并推送 origin(`fad37dd`、`6ec2b5b`) | git 盘点 |
| promote 认得 public-scraper 产物 | `raw/profiles.jsonl` + `downloads/image_downloads.jsonl` 布局、四段多级任务根(省/市/级/单位)解析均正确 | `cloud/promote_service.py` |
| 契约五大主项全部已实现 | 终落点路径拼装、people 四字段+no_photo 建档、safe 默认+turbo 显式允许、姓名净化闸门、禁写清单是**代码硬保护**(非纯文档纪律) | 契约核查 |
| Vision 核心合同成立 | 密钥只从 secret 文件/env 读且无泄露路径;UPDATE 列严格限于 `person_count`/`visual_*`/`vision_*`,不碰来源 `gender`;`force` 语义正确;分类失败真 fail-open;recrawl 必须 `confirm=true` 且永不自动 start | Vision 专项 |
| 安全基线良好 | 无硬编码密钥;敏感产物全部被 .gitignore 覆盖未跟踪;SQL 全参数化;`/api/v1` 统一 Bearer 门;图片预览路由有 resolve+根前缀校验+选角拦截;容器只读挂载、非 root | 安全审计 |
| 正式部署链已存在 | `package_d2i_cloud_release.py` → `deploy_cloud_release_to_nas.py`:打包→上传→releases/时间戳→切 current→compose 重建→健康轮询→冒烟→**失败自动回滚**,host/user/凭据均可 env 覆盖 | 部署盘点 |

---

## 2. 问题清单

### 2.1 高危(阻塞主线或单点风险)

| # | 问题 | 根因 / 位置 | 修法 |
| --- | --- | --- | --- |
| H1 | **本地 5 个提交未推送**(vision 队列、无 token 前端、部署安全门/回滚,`485d663..2999ea3`,已部署 NAS 但只存在本机 git) | `git status -sb`:ahead 5 | `git push`(待确认后执行) |
| H2 | **T0 跑完不自动落终点**——不是 promote 不认布局,是触发条件够不着:`_should_auto_finalize` 要求 live status==`completed`,而"已完成"要求**每个** profile 都有 metadata ok;无图人/元数据失败即永远 stopped,自动 promote 永不触发(手动 finalize 却能成功) | `cloud/queue_service.py:360-372`、`services/task_service.py:333-356` | 放宽触发:进程不在跑且 images>0 且静止超 N 分钟即允许一次 promote;或 metadata_ok 门槛只统计有图人 |
| H3 | **爬虫进程"不退出"是僵尸进程**:queue_service 用 `Popen` 启动后从不 `wait()/poll()`,Linux 上子进程退出即成 `<defunct>`;而 `session_running` 用 `os.kill(pid,0)` 判活,对僵尸也返回存活 → 永远 running | `cloud/queue_service.py:756`、`services/desktop_scraper_backend_service.py:194-201` | 保留 Popen + daemon watcher 线程 `proc.wait()` 回写退出码;判活改 psutil 并排除 zombie;爬虫收尾 `os._exit` 兜底 |
| H4 | **姓名净化双拷贝实质不一致**:browser 模式(`run_public_scraper.py:327-383`)还是旧宽松版——裸职务"市长"、导航词"魅力狮城"能过,`_prefer_person_name` 会回落原始文本(正是合同 §7 明令禁止的沧州反例);requests_jsl 模式才是严格版 | `run_public_scraper.py:327-383,3401` vs `public_profile_spider.py:61-151` | 抽 `scraper/name_sanitizer.py` 共享模块,以 spider 严格版为准;`run_public_scraper.py` 直接 import 并删两段拷贝;顺带统一 `llm_enricher.py:11` 第三份 |
| H5 | **Vision 外呼无路径白名单**:`POST /api/v1/ai/vision` 的 path 参数可指 NAS 任意可读图片(含 `选角/`),base64 后发外部 Grok;preview 路由有 `_is_protected_path` 防护,vision 完全没有 → 数据外发边界缺口 | `cloud/vision_service.py:63,113`、`cloud/api.py:540` | `resolve_image_path` 入口加根白名单(portrait_root/任务根),复用 `_is_protected_path` 拦选角/备份 |
| H6 | **NAS 侧 d2i CLI 是坏的**:`install_d2i_cloud_cli_on_nas.py` 生成的 bash 含悬空 `fi`,`set -euo pipefail` 下必语法报错;近期装过则线上 CLI 已失效 | `deploy/nas/install_d2i_cloud_cli_on_nas.py` CLI_SH 模板 | 删悬空行 + 安装前 `bash -n` 自检;下次发布重装覆盖 |
| H7 | **打包清单引用 git 未跟踪文件**:`package_d2i_cloud_release.py:33` 列了 `titi_metadata_schema.py`(untracked),`services/image_rendering.py` 同样;新 clone 打包必失败,NAS 在跑仓库里没有的代码 | `deploy/nas/package_d2i_cloud_release.py:33` | 两个文件纳入 git 并提交 |
| H8 | **鉴权口径三处打架**:代码=条件 Bearer(设 `D2I_WEB_TOKEN` 即强制);契约=NAS 暴露必须开;而 SKILL.md/cloud README/NAS 侧 SKILL 都说"无 token";`cloud/cli.py` 干脆不带 Authorization → NAS 开 token 时全线 401 | `cloud/api.py:25-37`、`cloud/cli.py:31-68`、契约 §鉴权 | 文档已于本日改回条件表述(见 §4.1);`cli.py` 补读 `D2I_WEB_TOKEN` 发 Bearer;**最终口径待拍板(见 §5-D1)** |

### 2.2 中危(影响正确性 / 质量)

| # | 问题 | 位置 | 修法 |
| --- | --- | --- | --- |
| M1 | metadata 审计默认必填 `photo_taken_at`/`age_at_photo`,政府头像天然无 EXIF → **人人进 review,review 变噪音库** | `run_public_scraper.py:5336-5376` | 政府类模板统一写 `rules.metadata_audit_required_fields: ["gender"]`(君合模板已用此口子);中期把这两项移出默认清单 |
| M2 | 磁盘 `reconcile_report.json` **没有 `run_state`**(只随 stdout 打日志),依赖磁盘判完成的消费方读不到 | `run_public_scraper.py:5586-5604` vs `6351-6396` | `run:6368` 后补一次 save_json,让磁盘与日志一致 |
| M3 | 24 个模板仅沧州 1 个全达标:23 个缺 `crawl.speed_tier`,22 个缺 `admin.province/city`;多数把 `image_url` 放 required(违背"无图不丢人");7 对文件完全重复 | `scraper/templates/` | 批量补齐 speed_tier=safe + admin;逐站确认 image_url 必填;重复对按 merge+stub 收敛,加 site_name 唯一性校验 |
| M4 | 速度档语义两处违契约:建队 `overwrite_existing=True` 把模板自定义 crawl 数字全部重置;模板档继承分支永不可达(默认串"safe"非空),模板 standard 被静默降 safe | `queue_service.py:694-699,689-693`、`speed_tiers.py:86-90` | 建队改 overwrite_existing=False;API/CLI 默认改空串=继承模板 |
| M5 | `no_photo` 去重用**子串匹配**,姓名互为后缀(李小明/小明)会漏建档;`_index_profiles` 按 name 索引,同名不同人互相覆盖 | `promote_service.py:537,224` | 精确 (name, detail_url) 键;同名以 detail_url 为主键 |
| M6 | Vision 同名回退:person_id 缺失时按 name-only(`ORDER BY rowid DESC LIMIT 1`)定位,同名官员会写错人/误跳过 | `vision_service.py:236-239,350-354` | name 回退强制附加 unit/city 约束;多行命中返回 ambiguous 待人工 |
| M7 | pump 双跑竞态三连:sync pump 无守卫、requeue_stale 无时限(会把在跑作业打回 queued)、`run_vision_job` 接受 running 态;background 启动检查与置位不在同一临界区 | `vision_service.py:1962-2010`、`jobs_db.py:549-573` | requeue 加 staleness 阈值;持锁置位;run 仅接受 queued |
| M8 | cancel 语义虚:队列 cancel 只置暂停旗标不杀进程;vision cancel 不中断执行且终态会覆写 cancelled | `queue_service.py:874-881`、`vision_service.py:1806-1835` | 按 pid 发终止信号;vision 循环加协作取消 + 终态 `WHERE status='running'` 守卫 |
| M9 | 手动 finalize 绕过 `_AUTO_FINALIZING` 防重,可与 reconciler 自动 promote 并发写同一 people/终落点;锁全是进程内,多 worker 即失效 | `queue_service.py:812-830,407-458` | 防重下沉到公共入口;远期用 jobs.sqlite 租约 |
| M10 | ~6000 行六月旧工作(desktop-next 26 文件 + 根目录名条/元数据)长期悬在工作区;`data/browser_profiles/`(含浏览器登录态)与 coverage PNG 原先未 gitignore,一次 `git add -A` 即泄漏到公开 GitHub | git 盘点 | .gitignore 已于本日补上(见 §4.1);旧工作按主题分批提交或明确放弃(见 §5-D3) |
| M11 | 部署工艺:12 个一次性脚本各自硬编码凭据路径+paramiko 直连(改一处要改 12 处);AutoAddPolicy 跳过主机指纹;sudo 密码拼进命令行(NAS ps 可见);无 runbook,发布史只在被 ignore 的本机目录 | `deploy/nas/dist/*` | 抽 `deploy/nas/nas_client.py` 公共模块;改密钥认证+固化指纹;sudo 走 stdin;写 `docs/d2i_cloud_release_runbook.md` + git 跟踪的 DEPLOY_LOG.md |
| M12 | promote 的 `resolve_admin_context` 五段式(省/市/区县/级/单位)分支与四段式代码相同 → 区县级会把区县当市、市当省 | `promote_service.py:158-164` | 五段按 parts[-5]/-4/-3 解析并填 county;或强制区县模板带完整 rules.admin |

### 2.3 低危 / 加固(择机顺手修)

- cloud/ 全无 logging,73 处宽泛 `except Exception` 多处静默吞(`queue_service.py:792`、`api.py:484` 等)→ 引入模块级 logger。
- CORS `allow_origins=['*']` + `allow_credentials=True`(`api.py:138-144`)→ 收敛为 `https://d2i.517411.xyz` + 本地调试源。
- Bearer 比较非常量时间(`api.py:37`)→ `hmac.compare_digest`。
- people 表缺 `source_url`/`source_page_image_status` 列时静默丢字段(`promote_service.py:596,377`)→ 加入必需列校验。
- 少数民族/超长姓名白名单未实现(spider 硬编码 2-4 字)→ `rules.allowed_long_names`。
- 裸 `__hdd_prebind` 段不在禁写标记;downloads 兜底扫描的文件名 stem 无姓名复验(`promote_service.py:30,234-249`)。
- vision:dry_run 也真调 Grok 计费;瞬时 API 故障与真实图片问题同入 must_recrawl 桶;写 `last_checked_at` 轻微越界;claim 非原子(单进程现状无害)。
- reconciler:running 布尔无失效恢复(线程死了无法重启);每轮全量 enrich 500 行,NAS I/O 放大。
- 发布冒烟硬编码沧州队列 ID,队列清理后门禁静默缩水。
- 根 README 停留在旧版 Tk 桌面,未提 cloud/ 主产品,残留旧机器路径(`README.md:123`)。

---

## 3. 迭代方案(按阶段,均走 Cloud 产品路径迭代,不开旁路脚本)

### 阶段 A:闭环收口(首批,目标"跑完自动落到角色肖像+people,进程干净退出")

> **2026-07-27 NAS 验收收口:** 代码 1–5 已推 origin;正式发布链 `DEPLOY_OK 20260727_011915`;CLI 重装;沧州小队列自动 promote 通过。

1. ✅ `git push` 已推 feature 分支;H7 两个打包文件此前已入 git。
2. ✅ 修僵尸进程(H3):`register_process_watcher` + 判活查回收表;`run_state` 落盘(M2)已接通。
3. ✅ 放宽自动 finalize 触发(H2):静止 N 分钟(默认 10,`D2I_AUTO_FINALIZE_QUIET_MINUTES`)+ images>0 可触发;政府模板 `metadata_audit_required_fields:["gender"]`(M1)已批量写入。
4. ✅ 统一姓名净化模块(H4):`scraper/name_sanitizer.py`,spider / run_public_scraper / llm_enricher 三处共用严格版。
5. ✅ Vision 外呼路径白名单(H5)+ CLI Bearer(H8 代码侧)+ 安装器悬空 `fi`(H6 代码侧)。**NAS 已重装 CLI。**
6. ✅ **验收(2026-07-27):** NAS 正式发布后跑沧州小队列 `q_0a56f3e21cac`:进程退出 → completed → 自动 promote `candidates=6/promoted=6/people_update=6` → 角色肖像 + people。顺手修:compose 恢复 `D2I_WEB_TOKEN`、发布冒烟带 Bearer、空 promote 不再锁防重。

### 阶段 B:质量与模板库(阶段 A 验收通过后)

1. ✅ 模板库批量达标(M3,首轮):现有模板补 `speed_tier=safe`、政府类 audit 字段、`image_url` 移出 required;全库 lint `tests/test_a5_templates_lint.py`。全国性站点 admin / 残留重复对仍可能需人工。
2. ✅ 速度档语义修正(M4):空串继承模板档;建队 `overwrite_existing=False`。
3. ✅ 同名/去重正确性(M5、M6):promote 精确 `(name, detail_url)`;vision name 回退强制 unit/city,多命中 ambiguous。
4. ✅ 区县五段路径(M12)+ 覆盖图谱 unit `kind`(F7)。
5. ✅ 结果库 SQLite 索引层(F5 长期方案):`library_items` 表 + `POST /api/v1/library/reindex`;`library_list` 有索引走 SQL 真分页,空索引回退文件扫描。
6. ⏳ 按覆盖图谱推进下一批城市模板(产品路径,非代码债)。

### 阶段 C:并发与运维加固

1. ✅ pump 双跑/stale 阈值 + cancel CAS + 进度分片回写(F1/F2/F4,即 M7/M8 主体);dry_run 也走去重(F3)。
2. ⏳ 手动 finalize 与自动 promote 防重下沉(M9 未完);logging + people 列校验仍待。
3. ✅ CORS 收敛 + Bearer `hmac.compare_digest`(低危已顺手做)。
4. ⏳ 部署工艺收敛(M11):nas_client / runbook / DEPLOY_LOG / 冒烟去硬编码 —— **下一步 NAS 发布前优先**。

### 阶段 D:文档整理(见 §4;仓库侧已部分完成,知识库侧待确认后执行)

---

## 4. 文档整理方案

### 4.1 仓库侧 —— 本日已完成 ✅

- `cloud/skills/d2i-cloud/SKILL.md`:鉴权改回**条件表述**(设 token 即强制 Bearer,`auth_enabled` 以 `GET /api/v1/status` 为准),curl 示例块加 Bearer 说明。
- `cloud/README.md`:恢复 `D2I_WEB_TOKEN` 环境变量条目。
- `docs/模板生成与使用SOP.md`:顶部加"Cloud 合同优先"横幅(required_fields 口径:合同只强制 name+detail_url,无图不丢人)。
- `.gitignore`:补 `data/browser_profiles/`、`data/coverage/*.png`。

### 4.2 仓库侧 —— 待做

- 根 README 加产品导览段(指向产品契约 / cloud/README / desktop-next),修旧路径。
- 山东公开官员采集系列 5 份(6 月)加"终落点口径已被 Cloud 合同取代"横幅后入 git;desktop-next 迁移系列 8 份按 merge+stub 收敛为 2-3 份。
- 新增 `docs/d2i_cloud_release_runbook.md`(阶段 C)。
- docs/ 下 10 份 untracked 文档分批入 git。

### 4.3 知识库侧(feiniu-nas)—— 方案已定,**动手前需你确认**

| 动作 | 对象 |
| --- | --- |
| 补记 T0 实测结果 | `D2I_待下载清单` 末尾加"T0 已完成"附录(沧州 6 人 6/6/6;当时 public-scraper 自动落终点未接通→本方案阶段 A 收口);`前后端交接 §3`"生产入库基本未开始"与`抓取图谱 §1.3`"仅 smoke"各改一行 |
| 合并+留存根 | `D2I_对面开工测试指令_20260726` 使命已完成(90% 与待下载清单/前后端交接重叠)→ 内容并入待下载清单,原文改 3 行 stub |
| 加降级 stub 头(仿 7/16 文档范式) | 7/25 三份:`深度整理与缺口方案`、`SSD迁移方案B`、`已入库核实清单` → 指向 `分类整理执行记录_20260726` 为现状权威 |
| 小修 | `部署记录`头部更新时间改 07-26;`权威方案` §3/§4 收编 7/25-7/26 全部新文档 + 补 T0 里程碑 |
| 整理后阅读顺序 | 权威方案 → 产品契约(仓库) → 前后端交接 → 部署记录 → 待下载清单(含 T0 结果) → 分类整理执行记录 |

---

## 5. 决策点执行情况

| 决策 | 结论 | 状态 |
| --- | --- | --- |
| D1 鉴权 | **已改口(2026-07-27):NAS 关闭应用层 Bearer**,只靠 Cloudflare Access;前端不再要 token。条件 Bearer 代码保留给本地/显式开启 | ✅ 线上 `auth_enabled=false`(`releases/20260727_014105`) |
| D2 git push | 推 feature 分支 | ✅ tip `319129f` 已推 `origin/feature/d2i-cloud-mvp` |
| D3 六月旧工作 | 封存归档分支;打包必需文件留 feature | ✅ 此前已执行 |
| D4 知识库整理 | 合并留桩/补记,无删除 | ✅ 此前已执行 |

**仍待拍板 / 待执行:**

- ✅ NAS 正式发布 + reindex + CLI 重装 + 阶段 A 第 6 步自动 promote 验收(见 §8.5 / §8.6)。
- ✅ 双主题前端(经典默认 / 观测台可选)已推 GitHub 并随 `20260727_014105` 上线。
- 是否开阶段 C 剩余项(M9 防重下沉、logging、M11 部署工艺)。
- 已知跟进(非阻塞):workspace KPI/`items` 在 registry 仅 1 个 active_root 时可能串台展示;东营模板列表 0 命中需单独修选择器;`images_only_with_record` cleanup 会在 promote 后清 raw(沧州验收已复现);promote 缺 gender 时落 `未知/` 并可能覆盖 people.gender——应用已有 男/女 终落点回填,勿写空。

---

## 6. 前台(Web 控制台)专项 —— 2026-07-26 晚补充

> 同日晚对 `cloud/web/` 做了静态审计(5 路并行)+ 本机浏览器实测(:8791)。**纯前端可修的已全部修完并逐页验证**(四页零控制台报错);后端联动项见 §6.2。**双主题前端已随 `20260727_014105` 上 NAS**(经典默认 / 观测台可选)。

### 6.1 本轮已修(纯前端,已本机验证 ✅)

| 文件 | 修了什么 |
| --- | --- |
| `common.js` | token 死锁根因:`promptToken` 原为一次性标志,401 永不再弹框;改为可重复提示(1.5s 防抖)。四页 `api()` 的 401 分支统一改为"先清掉旧 token 再重新索要" |
| `app.js` | ①「服务状态」被轮询竞态刷成「异常」的确认级 bug(`renderOverview()` 无参调用时不再触碰 statService);②快速切换队列时 items/详情/日志串台(全部 await 后加 `selectedId` 守卫 + 预览自增序号);③ finalize/cancel 加二次确认(finalize 明示写入 角色肖像+people 不可撤销);④创建队列提交按钮防双击;⑤ `detail_url` 加 http(s) 协议白名单 |
| `coverage.js` | ①选中节点导致整树塌陷(原展开判断是哈希前缀比较,毫无层级意义)→ 改 `expandedIds` 集合记忆展开态,选中只做就地高亮不重建树;② enqueue 确认文案改用中文动作名+节点名,并按「立刻开跑」勾选提示 |
| `vision.js` | ①「运行本批」对 failed/cancelled 可点但后端必拒 → 收紧为仅 queued;②泵运行中禁用「启动泵/跑 1 批」;③四个真实计费入口(运行本批/启动泵/跑 1 批/重跑失败项)全部加 confirm 并明示"调用 Grok 产生真实计费";④详情轮询竞态加 `selectedId` 守卫(防止对"看到的不是它"的批下危险操作) |
| `library.js` | 预览加载竞态(快速切换条目旧图覆盖新选中)加序号守卫;`detail_url` 协议白名单 |
| `styles.css` | `.tag.running/.cancelled` 双定义冲突去重(cancelled 统一灰色,与 failed 红区分,已实测全部状态色各异);清死 CSS(`.linkish`/`.help`/`.sheet.narrow`/`--radius-sm`/重复 `.muted-hint`);`.header-actions` 换行防窄屏溢出;修手机端 vision 表单仍 3 列的断点失效 |
| `vision.html` | 补上其他三页都有的内联 Access 门禁探针 + `<script onerror>` 兜底(此前 vision 页被 Access 拦截时只会白屏) |
| `common.js`(二轮) | **公共运行时抽取**:`api/escapeHtml/toast/looksLikeAccessGate/fmtTime/safeHttpUrl/fetchAuthorizedBlob/loadPreviewInto` 全部进 `window.D2I`;四页删拷贝,净删 ~295 行 |
| `styles.css`(二轮) | **视觉打磨**:5 档字号 token;色板收编;主按钮对比度 5.4–7.3:1;结果库布局 280px 筛选 + fluid 结果,预览 max-height 55vh |
| 失败下钻(#42) | **三层**:① reason chips 聚合;② `explainError` 中文目录(缺图/多人图/同名歧义…);③ 条目证据面板(判定+建议+JSON)。`queue_items` 增 `reason_counts` |
| 双主题(三轮) | **经典 = 默认实用壳**(`styles.css`);**观测台 = 可选实验皮**(`styles.observatory.css` + Lucide + 信号场 canvas)。`shell.js` 侧栏切换,写入 `localStorage.d2i_cloud_theme`;支持 `?theme=classic|observatory`。信号场仅观测台启用,降帧/减节点,并尊重 `prefers-reduced-motion` |

### 6.3 三主题用法(经典 / 日间观测台 / 暗色观测台)

| 主题 | 文件 / 键 | 何时用 |
| --- | --- | --- |
| **经典**(默认) | `styles.css` / `classic` | 日常运维;无 canvas,最跟手 |
| **日间观测台** | `styles.observatory-day.css` / `observatory-day` | 浅色实验皮(羊皮纸+铜+青绿) |
| **暗色观测台** | `styles.observatory.css` / `observatory` | 暗色实验皮 + 信号场 |

切换入口:`shell.js` 侧栏循环 **经典 → 日间 → 暗色**;偏好键 `localStorage.d2i_cloud_theme`;`?theme=` 可直达。
信号场=`#signalField` 装饰 canvas,仅两套观测台启用;减动效系统设置下关闭。
**卡死修复(必记):** Lucide 会把 `data-lucide` 换成 `svg.lucide`;enhance 必须 `data-d2i-icon` 幂等 + MutationObserver disconnect-while-mutate + debounce,否则主题切换/整页卡死。

### 6.2 前台暴露、但修复需动后端的问题(本轮未动)

| # | 问题 | 位置 | 归入 |
| --- | --- | --- | --- |
| F1 | 泵双跑双计费 | ✅ 已修:`requeue_stale` 移到泵锁之后 + 30min 阈值;同步路径 busy 直接返回;前端泵运行中禁用按钮 | 阶段 C |
| F2 | running 伪取消 | ✅ 已修:逐片读 status 遇 cancelled 停;终态 `expected_status='running'` CAS | 阶段 C |
| F3 | dry_run 更费钱 | ✅ 已修:dry_run 也走已识别去重(仍不写 people;计费本身无法前端免除) | 阶段 C |
| F4 | vision 进度恒 0% | ✅ 已修:循环内每 N 个 item 回写 done/ok/failed | 阶段 C |
| F5 | 结果库静默截断 | ✅ 过渡+长期都落地:响应 `truncated`/`per_queue_limit`;`library_items` SQL 真分页(空索引回退扫描) | 阶段 B |
| F6 | 筛选后下拉只剩当前队列 | ✅ 已修:only_queue 只过滤 items,summaries 全量 | 阶段 B |
| F7 | 单位节点缺 kind | ✅ 已修:unit dict / enrich 补 `kind=unit` | 阶段 B |
| F8 | 复核 KPI 人人进 | ✅ 代码侧:政府模板 audit 降为 `["gender"]`;真实队列上复核数下降需部署后观察 | 阶段 A |

---

## 7. 修订

| 日期 | 说明 |
| --- | --- |
| 2026-07-26 | 首版:9 路全仓体检产出;仓库侧文档修正(鉴权表述/SOP 横幅/.gitignore)同日完成 |
| 2026-07-26 晚 | 增补 §6 前台专项:纯前端修复 7 文件已完成并本机验证;后端联动问题 F1-F8 归入阶段 A/B/C |
| 2026-07-26 夜 | 阶段 A 代码收口(工作流中断后本地验证全绿并分 6 笔提交):H2/H3/H4/H5、M2/M4/M5/M6/M12、F1–F7 落地;11 个 tests/test_a* 全 PASS;NAS 未部署 |
| 2026-07-26 深夜 | 前端公共层抽取(common.js D2I 运行时,净删 ~295 行)+ 视觉打磨(5 档字号/色板 token/主按钮对比度≥5.4/结果库 280+fluid 布局);结果库 SQLite 索引层 library_items 落地(真分页,空索引回退文件扫描,POST /library/reindex) |
| 2026-07-27 | 失败原因三层下钻(#42): reason chips + 错误码中文目录 + 证据面板; queue_items 增 reason_counts |
| 2026-07-27 收口 | 本轮 14 提交已推 origin;§3 阶段 A/B/C 勾选更新;§5 决策执行表;§6.2 F1–F8 全标已修;新增 §8 落地总账与 NAS 发布 checklist |
| 2026-07-27 NAS | 正式发布 `20260727_011915`;恢复 compose `D2I_WEB_TOKEN`+auth 冒烟;reindex 36;CLI 重装;`q_0a56f3e21cac` 自动 promote 6/6;空 promote 防重解锁 |
| 2026-07-27 主题 | 双主题:经典默认 + 观测台可选(`styles.observatory.css` / `shell.js`);信号场仅观测台;Lucide 图标 |
| 2026-07-27 再发 | 推 tip `319129f`;NAS `DEPLOY_OK 20260727_014105`;Access-only 鉴权(`auth_enabled=false`);双主题静态资源已在 :8787 验证 |
| 2026-07-27 分支 | **长期开发线升格**:`feature/d2i-cloud-mvp` → `cloud`(已推 `origin/cloud`);本地仓 `D:\vibe coding\d2ilite` |
| 2026-07-27 前端修 | **卡死 + 主题切换**:`shell.js` MutationObserver/Lucide 死循环(lucide 把 `data-lucide` 换成 `svg.lucide` 后仍反复注入图标);改 debounce + disconnect-while-mutate + `data-d2i-icon` 幂等;主题样式用 pending link 原子切换 |
| 2026-07-27 NAS 再发 | 外网经 VMISS 发布 **`DEPLOY_OK 20260727_104334`**;`current`→`releases/20260727_104334`;critical_gate + theme_assets_ok;`shell.js` 含 hasIcon/d2i-icon |
| 2026-07-27 日间主题 | 第三皮 **日间观测台** `styles.observatory-day.css`(羊皮纸+铜+青绿);侧栏循环 经典→日间→暗色;信号场 day 调色;stylesheet settle 80ms 兜底 |
| 2026-07-27 视觉闭环 | 失败原因中文化;按失败类分流 requeue;跑完 auto-route→retry-auto / 建议重抓 inbox;`GET /ai/vision/recrawl-inbox` |
| 2026-07-27 Hermes skill | 完整 `d2i-cloud` skill+AGENT_BRIEF 装到 `nas-main`;CLI 补 vision requeue/recrawl-inbox/auto-route;归档旧 `d2i-image-worker` |
| 2026-07-27 人物标记 | **people 照片工作流** `cloud/people_workflow.py`:no_photo / hold / unusable / resume;inventory 闸门;UI+CLI;`auto-route` 对过小/截断自动 unusable |
| 2026-07-27 当前线上 | GitHub `origin/cloud` tip **`0829aec`**;NAS **`DEPLOY_OK 20260727_164445`**(`current`→`releases/20260727_164445`);`auth_enabled=false`;health ok |

---

## 8. 2026-07-26~27 落地总账

### 8.1 提交范围

分支:**`cloud`** → `origin/cloud`(长期开发线;由 `feature/d2i-cloud-mvp` 升格)。`feature/d2i-cloud-mvp` 保留为历史 tip 指针。

| 主题 | 代表 commit |
| --- | --- |
| 失败下钻 | `feat(cloud/web): failure drill-down — reason chips, human labels, evidence panel` |
| 结果库索引 | `feat(cloud): SQLite library_items index for real cross-queue paging` |
| 视觉打磨 | `style(cloud/web): visual polish — type scale, tokenized colors, library layout` |
| 公共层 | `refactor(cloud/web): lift shared helpers into common.js D2I runtime` |
| 阶段 A 测试 | `test: stage-A unit coverage for queue/name/vision/promote/templates` |
| 模板库 | `chore(templates): batch-align speed_tier/admin/audit fields` |
| promote/coverage | `fix(cloud): promote exact-key dedup + five-segment admin; coverage unit kind` |
| Vision 安全 | `fix(cloud/vision): path allowlist, pump double-run guard, cancel CAS, progress flush` |
| 姓名净化 | `fix(scraper): unify name sanitizer + persist reconcile run_state` |
| 队列服务 | `fix(cloud): stage-A queue service — zombie reaper, auto-finalize, speed tier, library truncation` |
| 前台加固 | `fix(cloud/web): frontend hardening — 401 re-prompt, race guards, billing confirms…` |
| 观测台实验皮 | `feat(cloud/web): experimental Noir Observatory shell — Lucide, dark canvas, display type` |
| 双主题切换 | `refactor(cloud/web): split Observatory into optional theme with classic default` |
| Access-only 发布姿态 | `fix(deploy): Access-only NAS auth posture + dual web themes ready for release` |
| 卡死/主题切换修复 | `fix(cloud/web): break Lucide/MutationObserver freeze; harden theme swap` |
| 外网部署跳板 | `feat(deploy): support VMISS jump for out-of-LAN NAS cloud release` |
| 日间观测台 | `feat(cloud/web): add Day Observatory light theme (third skin)` |
| 视觉失败中文化 | `fix(cloud/web): humanize vision failure reasons (too-small / truncated)` |
| 失败类分流重跑 | `feat(cloud/vision): split requeue by failure class (retryable vs must-recrawl)` |
| 跑完自动分流 | `feat(cloud/vision): auto-route finished jobs into retry / recrawl streams` |
| Hermes 完整 skill | `feat(hermes): full d2i-cloud skill install + vision CLI for nas-main` |
| 人物照片标记 | `feat(cloud): photo workflow marks — no_photo / hold / unusable / resume` |
| 长期分支 | `cloud` 升格自 `feature/d2i-cloud-mvp` |

### 8.2 测试

- `tests/test_a1_*.py` … `test_a5_templates_lint.py`:全 PASS(直跑 python)
- `tests/test_library_index.py`:索引写入/查询/空索引回退 PASS
- `tests/test_error_catalog.js`:错误码中文目录 PASS
- 本机浏览器(:8791)四页回归零控制台报错;结果库三栏比例与主按钮对比度已复测;失败 chips + 证据面板已实测

### 8.3 上线 checklist

1. ✅ 走既有 `package_d2i_cloud_release.py` → `deploy_cloud_release_to_nas.py`(失败自动回滚;外网默认 `D2I_NAS_VIA_VMISS=1`)。
2. ✅ 发布后 library `source=index`(36 items 量级,见各次发布冒烟)。
3. ✅ NAS 侧 `d2i` CLI 重装可用(完整 skill + vision/people 子命令)。
4. ✅ 政府小队列自动 promote 验收(见 §8.5)。
5. ✅ 三主题前端上线(见 §8.6 / §6.3 / §8.7)。
6. ✅ 视觉 auto-route + 建议重抓 inbox + people 工作流标记上线(见 §8.7)。
7. ⏳ 观察复核 KPI / vision 进度条 / 失败 chips 线上抽查;三主题实机手感;people 标记批量使用节奏。

### 8.4 明确未做 / 不在本轮

- M9 多 worker 租约;cloud 全量 logging;M11 部署工艺大收敛。
- 根 README 产品导览。
- 不恢复 desktop-next 归档;不碰 山东公开官员 / 选角 / 禁写清单。
- 东营模板 0 列表命中 / workspace items 串台展示 / promote 空 gender 覆盖 —— 记入后续,不挡阶段 A 收口。

### 8.5 2026-07-27 NAS 发布与验收实录(阶段 A)

| 项 | 结果 |
| --- | --- |
| 发布 | `DEPLOY_OK 20260727_011915`(中间 `20260727_010528` 无 token 门、`20260727_011000` 因冒烟无 Bearer 自动回滚后已修) |
| current(当时) | `/vol1/1001/d2i-cloud/releases/20260727_011915` |
| 鉴权(当时) | 一度恢复 compose `D2I_WEB_TOKEN`;后按 D1 改口改为 Access-only(见 §8.6) |
| reindex | `POST /api/v1/library/reindex` → items=36, library `source=index` |
| CLI | `install_d2i_cloud_cli_on_nas.py` 重装;host/`hermes-nas` 可用 |
| 自动 promote | 队列 `q_0a56f3e21cac`(沧州市政府领导 safe):`completed` → auto promote counts `candidates=6,promoted=6,people_update=6`;`final_base=/runtime/portrait/政府/河北省/沧州市/市级/沧州市人民政府` |
| 过程修复 | ① 打包 `≈` 在 GBK 控制台炸 → 改 `~` + stdout utf-8;② 冒烟补 auth 分支;③ 空 promote(`candidates=0`)不再写死 `promoted` 防重 |

### 8.6 2026-07-27 双主题 + Access-only 再发

| 项 | 结果 |
| --- | --- |
| GitHub | 当时推 `origin/feature/d2i-cloud-mvp` tip **`319129f`**;现已升格为长期线 **`origin/cloud`**(含后续卡死/主题修复) |
| 发布 | **`DEPLOY_OK 20260727_014105`** |
| current | `/vol1/1001/d2i-cloud/releases/20260727_014105` |
| 鉴权 | compose **默认不注入** `D2I_WEB_TOKEN`;冒烟 `auth_enabled=false`;critical gate 在 auth 关时接受开放 status,auth 开时仍要求 401 |
| 前端资源(LAN `:8787`) | `styles.css`(经典)、`styles.observatory.css`(观测台)、`shell.js`、四页 HTML 的 `d2iThemeStyles` 均 200 |
| 主题用法 | 侧栏底部切换「经典 / 观测台」;或 `?theme=classic` / `?theme=observatory`;偏好存 `localStorage.d2i_cloud_theme` |
| 信号场 | 仅观测台主题启用的装饰 canvas(网格+节点连线);经典主题不跑;减动效系统设置下关闭 |
| 冒烟摘要 | health/status/library/coverage/vision critical_gate_ok;library `source=index` total 36;queues 8 / promoted 7 |

### 8.7 2026-07-27 下午~傍晚:视觉闭环 + 人物标记 + 当前线上

| 项 | 结果 |
| --- | --- |
| GitHub | 长期线 **`origin/cloud`** tip **`0829aec`**(本机与 remote 一致,工作区干净) |
| 发布链 | 外网经 VMISS:`104334`(卡死修复) → `113120`(视觉分流+日间主题) → `114829`(Hermes skill) → **`164445`(people 标记)** |
| 当前 current | `/vol1/1001/d2i-cloud/releases/20260727_164445` |
| 健康(VMISS 探测 16:5x) | `/health` ok;`status` product=d2i-cloud,`auth_enabled=false`,queues=8 completed=7 promoted=7,vision available |
| 视觉闭环 | 失败原因中文化;requeue 按 retryable / must-recrawl 分流;job 结束 auto-route 产 retry-auto 子批 + 日建议重抓 JSONL;`GET /ai/vision/recrawl-inbox` |
| Hermes | `nas-main` 装完整 `skills/d2i-cloud`(SKILL+AGENT_BRIEF);`d2i vision requeue|recrawl-inbox|auto-route`;`d2i people mark|list…`;旧 image-worker skill 已归档 |
| 人物照片工作流 | `cloud/people_workflow.py` 复用既有 people 列,不另起表。动作:**no_photo / hold / unusable / resume**;inventory 默认跳过闸门态;视觉页证据/收件箱按钮;tiny/truncated auto-route 自动 unusable |
| 主题 | 三皮:经典 / 日间观测台 / 暗色观测台;侧栏循环;卡死根因与修法见 §6.3 |
| 发布命令(本机) | `py -3 deploy/nas/deploy_cloud_release_to_nas.py`(默认 `D2I_NAS_VIA_VMISS=1`);成功只认 `DEPLOY_OK <stamp>` |
| 回滚 | 失败脚本自动切回 previous_release;手动:`current` 软链指向上一 stamp + compose recreate |
